// if
const user={
    username:"sam",
    age:20,
    location:"delhi",
    salary:20000
}
function greeting () {
console.log(`hello ${user.username}`)
}
if(true){
    // greeting();
}
const bankAccount = {
  accounttype:"Saving",
  holdername:"raju",
  minmumBallance: 5000,
  branch: "Bhopal",
  currentBallance: 50000
}

if(bankAccount.currentBallance>=10000){
    // console.log(`account type is ${bankAccount.accounttype} and holder name is ${bankAccount.holdername}`);
}
